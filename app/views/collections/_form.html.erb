<%= simple_form_for(collection) do |f| %>
  <%= f.input :title,
              placeholder: 'enter the collection name',
              required: true,
              minlength: 1 %>
  <%= hidden_field_tag 'bottle', @bottle %>
  <%= f.button :submit %>
<% end %>
